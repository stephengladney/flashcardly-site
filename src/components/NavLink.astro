---
const { title, path } = Astro.props
---

<a class="link text-white font-bold font-poppins block mx-6" href={path}
  >{title}</a
>

<style>
  @media screen and (min-width: 1024px) {
    a {
      margin: 16px 0px;
      padding: 0.2em 0;
    }
  }
  a {
    /* margin: 16px 0px; */
    text-decoration: none;
    display: block;
    position: relative;
    padding: 0.35em 0;
  }

  a.link::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 0.2em;
    background-color: #fff;
    opacity: 0;
    transition: opacity 300ms, transform 150ms;
    opacity: 1;
    transform: scale(0);
    transform-origin: center;
  }

  a.link:hover::after {
    transform: scale(1);
  }
</style>
